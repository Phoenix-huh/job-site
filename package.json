{
  "name": "job-site-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev -- -p 3001"
  },
  "devDependencies": {
    "next": "16.2.3"
  }
}
